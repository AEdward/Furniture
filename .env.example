# MySQL connection. Point these at your own database (a local MariaDB/MySQL
# server, or the one started via `docker compose up -d`).
DB_HOST=localhost
DB_PORT=3306
DB_USER=furniture_app
DB_PASSWORD=furniture_dev_pw
DB_NAME=furniture
